# GitHub Token para publicación de releases (NO COMPARTIR)
# Genera uno en: https://github.com/settings/tokens
# Con permisos: repo (todos los sub-permisos)
GH_TOKEN=tu_token_aqui

# IMPORTANTE: 
# 1. Copia este archivo a .env
# 2. Añade tu token real en .env
# 3. NUNCA subas .env a git (ya está en .gitignore)
